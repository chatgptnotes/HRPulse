{
  "version": 2,
  "buildCommand": "npm run build --workspace=@hrpulse/frontend",
  "outputDirectory": "frontend/dist",
  "functions": {
    "api/[...path].ts": {
      "maxDuration": 60
    }
  },
  "rewrites": [
    { "source": "/((?!api/|uploads/).*)", "destination": "/index.html" }
  ]
}
